# 2026-09-23 관리자 재연결 고객 테스트 결과와 조치

## 고객 보고 (카톡 12:24~12:35, 캡처 묶음 `아카이브.zip`)

- 12:08 모바일 복구 코드(`…476b8f29`)로 노트북 관리자 앱에서 재연결 시도 → "요청을 완료하지 못했습니다. 네트워크 확인…" 3회
- 이후 Apps Script를 `main` 최신으로 새 버전 배포
- 12:21 두 주문 동시 선택 → "이전 기기가 서로 다릅니다", 12:22 한 건만 선택 → "주문 1건을 해당 기기로 재연결했습니다"
- 모바일 내 주문은 12:23·12:34 새로고침 후에도 "조회 가능한 참여 내역이 없습니다"
- 잘 보이던 노트북 내 주문이 두 건(아메리카노 10잔 번들·든든한 분식 세트)만 남고 둘 다 "주문 서버 반영 확인 필요"

묶음에는 9/15~9/22 캡처도 섞여 있다(관리자 인증 지연, 입금 상태 확인 필요, 9/21 상품 ID를 복구 코드 칸에 붙여 넣은 화면 등). 이미 조치된 건은 재조치하지 않았다.

## 운영 로그 (Vercel, 12:03~12:35 KST, `.vercel/feedback-20260923/all.jsonl`)

| 시각 | 경로 | 결과 |
| --- | --- | --- |
| 12:04:11, 12:04:14 | customer-orders publish | 404 `deal_not_found` ×2 |
| 12:08:34, 12:09:12, 12:09:43 | admin-ops `recovery_reassign` | 409 `invalid_action` — Apps Script 배포 전 |
| 12:19:58 | public-deals, customer-orders list | 502 `이름이 '복구 등록'인 시트가 이미 있습니다` — 새 버전 첫 요청 두 건이 시트를 동시에 만들려다 한쪽 실패 |
| 12:21:32 | admin-ops `recovery_reassign` | 409 `recovery_mixed_ownership` |
| 12:22:14 | admin-ops | 200 — 재연결 기록 성공 |
| 12:22~12:35 | customer-orders list | 전부 200 |

재연결은 서버에 기록됐고 이후 조회도 실패 없이 200이다. 즉 **조회는 성공했는데 결과가 비어 있다.**

## 판단

`getCustomerOrders_`는 **로그인 전화번호로 주문 행을 먼저 거른 뒤** 키(해시)·승계로 권한을 판정한다. 관리자 재연결은 전화번호와 무관하게 주문 ID만 결박한다. 따라서 휴대폰 사용자 앱의 로그인 번호가 그 주문의 번호와 다르면 재연결이 성공해도 목록은 비고, 화면 어디에도 그 이유가 보이지 않았다.

노트북 증상도 같은 설명과 맞는다. 남은 두 건은 앱 기본 예시 상품(`deal-kimbap`, `deal-cafe`)에 12:04에 새로 넣은 주문이고(서버가 공개 상품으로 모르는 상품이라 `deal_not_found`), 그 외 주문이 모두 빠졌다. 다른 번호로 다시 로그인한 경우의 모습이다.

**확정은 못 했다.** 운영 시트의 전화번호를 직접 대조하는 읽기는 이번 세션 권한 정책상 수행하지 못했다. 그래서 고객이 두 기기의 로그인 번호를 화면에서 바로 확인할 수 있게 먼저 바꿨다. 번호가 같은데도 비면 다른 원인이다(예: 재연결 직전 모바일 저장소가 다시 비워져 복구 코드가 바뀜).

## 조치 (`dd1302d`, 웹 배포 `dpl_9ctXqxa2a71Q5dFz8exmfob4BRvx`)

- 사용자 내 주문: "로그인 번호 010-XXXX-XXXX로 넣은 주문만 표시합니다" 안내
- 관리자 주문 목록: 주문별 번호 `010-****-뒤4자리` 표시. 번호가 다른 주문을 함께 재연결하지 못하게 막고, 확인창·성공 문구에 "그 기기가 이 번호로 로그인되어 있어야 보인다"를 명시
- 관리자 `invalid_action`: "Apps Script가 이 기능을 모르는 이전 버전" 으로 안내(기존: 네트워크 확인)
- 복구 코드 칸에 상품 ID를 넣으면 "상품 ID라 복구 코드가 아니다" 로 안내
- 서버가 `deal_not_found`로 거절한 예시 상품 주문: 빨간 "주문 서버 반영 확인 필요" 대신 "체험용 예시 상품 — 이 기기에만 남음"
- 자가 등록·복구 화면(`743c2bd`)도 이번 웹 배포로 운영 반영. 오늘 배포된 Apps Script가 `recovery_credentials`를 포함한다
- 콜렉터 `sheetByNameOrInsert_`: 시트 생성 경쟁에서 진 요청이 이미 생긴 시트를 다시 읽는다. **운영 반영은 다음 Apps Script 배포 때.** 시트가 이미 생겼으므로 지금 같은 오류는 다시 나지 않는다

검증: 단위·API·Apps Script 연계 611/611, 모바일 Chromium·WebKit 194/194, 빌드, 운영 5개 경로 200, 운영 자산 `index-oRkHIekw.js`(SHA-256 `0c26a7d3963aa963d2c803ca7fa9b514f1dc45aba52aa67e3c27bb01f0780d56`)에 새 문구 포함. 되돌리기: `dpl_HExd1tKWH6TgKY7sAgvX6uTSoxei`.

## 남은 일

1. 고객이 두 기기의 로그인 번호와 관리자 화면의 주문 번호를 알려 주면 원인 확정
2. 번호 불일치로 확정되면, 재연결된 주문은 로그인 번호와 무관하게 돌려주도록 콜렉터를 고칠지 결정(Apps Script 재배포 필요). 지금은 재배포를 요청하지 않았다
3. 모바일 목록이 뜨면 확인번호 등록 → 저장소가 비워진 뒤 자가 복구로 되살아나는지 확인 → 최종 검수 재요청

## 2차 확인 (카톡 14:05~14:10, `아카이브 2.zip`)

- 노트북: 01037474754로 다시 로그인하자 내 주문 전체가 정상 표시, 반영 오류 없음 → **노트북은 로그인 번호 문제로 확정**
- 관리자 목록의 두 경기미 주문 모두 `010-****-4754` → 모바일도 같은 번호. **모바일은 번호 문제가 아니다**
- 14:01 모바일은 회원 등록 화면부터 다시 시작(프로필까지 사라짐) → 사이트 저장소가 또 비워졌다. 12:22 재연결은 그 이전 키(`…476b8f29`)에 묶였으므로 함께 무효가 됐다. 12:23의 빈 목록도 그 사이 키가 바뀐 것으로 본다
- 13:59:45 `/api/recovery` 200: 노트북 확인번호 등록 성공
- 14:01:46, 14:02:27 `/api/recovery` 403 ×2: 모바일 되살리기가 확인번호 불일치로 거절. 캡처상 입력 자리 수가 12자리, 11자리로 달랐다
- 13:59~14:02 `order_ownership_unclaimable` 409 약 13건: 노트북 로그인 번호가 바뀌면서 그 번호의 옛(해시 없는) 로컬 주문이 한 번씩 재게시되다 거절된 것. 주문당 한 번이며 반복하지 않는다. 같은 시각 콜렉터가 바빠 관리자 인증 503 2건(재시도 후 진입) — 고객이 말한 "관리자 로그인 1번 에러"

관리자 인증 저장소 오류는 일부러 자동 재시도하지 않는다. 인증 시도 한 번이 15분 5회 상한을 소모하므로 자동 재시도는 잠금을 앞당긴다.

### 조치 (`2a11299`, 웹 배포 `dpl_Fu9UPjJXrQkyeWnV7FNd7o4z4cKG`, 자산 `index-BrqWBcls.js` SHA-256 `9a414e525e4ce9bab859f54a01a39f9b88ba7f60f28d823a8a76f8e5092d3ab2`)

- 확인번호 입력칸에 "숫자 보기", 등록 기기에 "확인번호(N자리) 등록됨", 되살리기 실패 시 "방금 넣은 숫자는 N자리". 로컬에는 자리 수만 저장
- 카카오톡 인앱 브라우저(UA `KAKAOTALK`)에서 내 주문 상단에 저장소가 비워질 수 있다는 안내와 `kakaotalk://web/openExternal` 기본 브라우저 열기 링크

검증: 613/613, 194/194, 빌드, 운영 5개 경로 200.

### 남은 구조적 한계

한 키는 승계를 하나만 가질 수 있다(`recoverySuccession_`이 둘 이상이면 모호로 보고 거절). 모바일이 노트북 등록을 되살리면, 옛 모바일 키의 주문(`order-1789458468283405584`)은 같은 키에 관리자 재연결로 더할 수 없다(`recovery_succession_exists`). 여러 승계를 합쳐 허용하려면 콜렉터 수정과 Apps Script 재배포가 필요하다.

## 3차 확인 (카톡 15:01, "사파리에서도 안됩니다")

- 고객은 iOS Chrome에서 01037474754로 로그인, 확인번호 칸에 **전화번호 01037474754**를 넣었다(숫자 보기로 캡처에 보임)
- 로그 13:45~15:15 `/api/recovery`: 등록 200 1건(13:59:45, 노트북), 되살리기 403 4건(14:01:46, 14:02:27, 14:58:55, 14:59:25). 모두 확인번호 불일치
- 노트북 등록 화면 캡처의 가려진 자리 수는 약 10자리로 관리자 PIN 길이와 같다. 등록한 숫자와 되살릴 때 넣은 숫자가 다르다고 판단한다. 같은 등록→되살리기 흐름은 실제 콜렉터 코드로 검증된다(`tests/recovery-api.test.js`)
- 고객이 "확인번호"를 전화번호나 관리자 PIN으로 이해하고 있다

### 조치 (`3a87ceb`, 웹 배포 `dpl_Gh7j91JTfuWNZDBMGvJFZSM5wQuQ`, 자산 `index-D1dIA9ks.js` SHA-256 `ffd2f0ae12de9160a665bd43c60f48cf95f7c41b1776968cb709ab226fa0ded7`)

- 전화번호 또는 그 일부(뒤 8자리 포함)를 확인번호로 등록하지 못하게 API·화면 양쪽에서 거절(`recovery_pin_matches_phone`). 전화번호는 사장님·그룹 화면에 보이는 값이라 보안상으로도 확인번호가 될 수 없다
- 등록·되살리기 두 화면에 "전화번호·관리자 PIN이 아니라 새로 정하는 숫자"를 명시

검증: 613/613, 194/194, 빌드, 운영 5개 경로 200.

## 4차 확인 (카톡 15:05~15:08, "노트북 확인번호, 핸드폰 번호 둘 다 안 됨")

로그 `/api/recovery` 전체(13:45~15:40):

| 시각 | 결과 | 비고 |
| --- | --- | --- |
| 13:59:45 | 200 | 노트북 등록 |
| 14:01:46, 14:02:27 | 403 | 모바일(카카오톡) 되살리기 불일치 |
| 14:58:55, 14:59:25, 15:04:27 | 403 | 모바일(Chrome) 되살리기 불일치. 14:59는 전화번호 입력 |
| 15:06:04, 15:06:51 | 200 | 노트북 재등록 — 같은 키라 기존 행의 확인번호만 교체 |
| 15:07:18 | 429 | 모바일 되살리기가 입력 제한에 걸려 **숫자 확인 전에 거절** |

재등록한 숫자로는 아직 한 번도 확인되지 않았다. 입력 제한은 연결(IP)당 15분 5회 실패, 차단 15분이며 설계대로 동작했다.

### 조치 (`01f6581`, 웹 배포 `dpl_39XCyuYM5DpFAmgr6zfbuV11uvUi`, 자산 `index-BPRnTo8z.js` SHA-256 `75f02af8c092fab6a4b85a8e283a4d30f092dc303ddc3b44025f5bb6e85a355c`)

- 429 응답에 대기 시간(`retryAfter`)을 싣고, 되살리기 화면에 "HH:MM 이후에 다시 시도"를 표시
- 테스트 추가: 같은 기기 재등록 시 새 숫자로만 되살아나고 옛 숫자는 거절, 제한 시 대기 시간 반환

검증: 615/615, 194/194, 빌드, 운영 5개 경로 200.

재등록한 숫자로 제한이 풀린 뒤에도 실패하면 등록·되살리기 경로의 운영 결함으로 보고 다시 조사한다.

## 5차 (카톡 15:21, "다시 순서 정해주세요. 노트북에 입력된 값은 연동되지 않음")

고객은 등록한 숫자를 넣었는데도 실패했다고 한다. 로그로는 입력한 숫자를 확인할 수 없으므로, 추정 대신 절차 안에서 판정되게 했다.

### 조치 (`f0c570b`, 웹 배포 `dpl_AwSFNbA5MCWtcr1n8EcJysyPNk7C`, 자산 `index-CtRBW2m6.js` SHA-256 `5df9c0bcadb18b55ab0586883d4b03c92b48e34d84e51eaea802e44d841c7f1b`)

- `/api/recovery` `action: 'verify'`: 되살리기와 같은 비교를 하되 승계를 기록하지 않는다. 맞으면 `{ verified: true, thisDevice }`(맞춘 등록이 이 기기 것인지), 틀리면 실패로 기록하고 403
- 등록한 기기의 내 주문에 "등록한 숫자 확인 → 맞는지 확인"
- 판정: 노트북 확인 통과 + 모바일 되살리기 실패 → 숫자 문제가 아니라 기기·전화번호 쪽 결함. 노트북 확인 실패 → 입력한 숫자가 등록값과 다름

검증: 616/616, 194/194, 빌드, 운영 5개 경로 200.

## 6차 — 근본 원인: 브라우저 비밀번호 자동 채움 (카톡 15:31)

고객 노트북 캡처: 확인번호·다시 입력 두 칸이 **이미 관리자 PIN으로 채워져 있었다**(고객은 "관리자 PIN으로 바꿔 두셨더라"고 이해). 확인번호 칸이 `type="password"`였고, Chrome은 이 사이트에 저장된 비밀번호(관리자 PIN)를 비밀번호 칸에 채우며 `autocomplete="off"`를 무시한다. 그래서 노트북 등록(13:59, 15:06)은 고객이 정한 숫자가 아니라 자동 채움된 관리자 PIN으로 이루어졌을 가능성이 크고, 모바일에서 넣은 다른 숫자·전화번호는 맞을 수가 없었다. **고객 입력 실수가 아니라 화면 결함이다.** 앞선 "입력 숫자가 다르다"는 안내는 결과적으로 맞았지만 원인을 고객 탓으로 돌렸다.

같은 캡처에서 노트북 등록은 `recovery_nothing_to_bind`(15:29:12, 15:30:33 409)로 거절됐고, 요약이 "확인번호 등록 —"(로컬 등록 기록 없음)이다. 15:06 등록 때와 다른 브라우저이거나 그 사이 저장소가 초기화된 것으로 보이며 확인하지 못했다.

### 조치 (`63cda25`, 웹 배포 `dpl_EKotEVk164o35GpQJLtxGnMW6gtT`, 자산 `index-alU1enS0.js` SHA-256 `9daaebf3dc54ff4d7547cecb51d0f17d2975d4a65ce53b2d04007a82c0a53bba`)

- 확인번호 칸을 `type="text"` + `inputMode="numeric"`로 바꾸고 가림은 CSS(`-webkit-text-security`)로만. 비밀번호 관리자 무시 속성(`data-lpignore`, `data-1p-ignore`) 추가. 비밀번호 칸이 아니므로 저장된 자격증명이 채워지지 않는다
- 검증: 617/617, 194/194, 빌드, 운영 5개 경로 200

### 남은 것

- 서버의 현재 등록 값(15:06)은 관리자 PIN일 가능성이 크다. 모바일에서 관리자 PIN으로 되살리기를 시도하도록 안내
- 되살린 뒤 관리자 PIN 변경 권장(확인번호로도 쓰였고 캡처로 공유됨)
- 등록 결박 집합(`recoveryBoundSet_`)은 주문 내역 현재 행만 보고, 조회는 현재 행 + 이벤트 로그 스냅샷을 본다. 조회되는 주문이 등록에서 빠질 수 있는 불일치다. 콜렉터 수정과 재배포가 필요하다

## 7차 (카톡 16:47~16:48, 모바일에서 관리자 PIN으로 되살리기 → "복구 서버에 연결하지 못했습니다")

- 16:47:09 `/api/recovery` 503 한 건. 같은 시각 `/api/collect` 504·503(16:46:18, 16:46:24, 16:47:00) — 콜렉터 포화
- 503은 `recovery_store_unavailable`. 콜렉터가 알려진 코드 외의 답(`collector_busy` 등)을 주거나 25초 안에 답하지 않은 경우다. 기존 코드는 `begin`만 `collector_busy`를 재시도했고, 숫자가 맞은 뒤의 `redeem` 기록은 재시도하지 않았다. 핸들러가 단계를 로그에 남기지 않아 **숫자 비교 전 실패인지 후 실패인지는 이 기록으로 구분되지 않는다**

### 조치 (`8af0e59`, 웹 배포 `dpl_qW2nMnH5dkSRRoEMY35Exdh6KYRA`, 자산 `index-hKOlA2b2.js` SHA-256 `f3b5eb8ddcfe202c6003bf38e9a98d35c267081f2d272e3c3a6a32225868c711`)

- `collector_busy`(스크립트 잠금을 못 잡아 아무것도 읽거나 쓰지 않은 상태)는 모든 복구 오퍼레이션에서 300·700·1500ms로 재시도
- 숫자가 맞은 뒤 기록이 끝나지 못하면 `recovery_redeem_incomplete`(503) — 화면에 "확인번호는 맞았습니다. 잠시 뒤 한 번 더"를 표시하고 실패 횟수를 올리지 않는다. 같은 요청의 재전송은 콜렉터가 같은 복구로 처리한다
- 실패 로그 `[recovery] request_failure {action, code, status, upstream}` — 전화번호·숫자·토큰은 남기지 않는다
- 검증: 619/619, 194/194, 빌드, 운영 5개 경로 200

### 별도 과제

콜렉터 포화(`/api/collect` 504·503 반복, 오전 관리자 첫 로그인 503 포함)는 하루 종일 관측됐다. 분석 이벤트 적재와 그룹 채팅 폴링이 같은 스크립트 잠금을 다투는 구조가 원인으로 보인다. 계약 범위의 채팅(약 5초 갱신) 품질과도 걸리므로 따로 조사가 필요하다.

## 8차 (카톡 19:18~19:19, 모바일 Chrome)

- 19:14:47 `/api/recovery` 503 `recovery_redeem_incomplete` — 숫자는 맞았고 콜렉터 기록이 25초 안에 답하지 않음(upstream 코드 없음 = 시간 초과)
- 19:15:39 `/api/recovery` 200 — **되살리기 성공.** 화면에 "주문 28건·그룹 8개·상품 7개를 이 브라우저에 다시 연결했습니다"
- 19:15:46, 19:16:05, 19:16:16, 19:16:37 `/api/customer-orders` list 200 — 그러나 고객 화면에 주문이 뜨지 않음
- 12:22 관리자 재연결 뒤와 같은 양상이다. 같은 브라우저·같은 키로 몇 초 뒤 조회했으므로 이번에는 키 소실로 설명되지 않는다

### 재현으로 찾은 결함

`tests/e2e/recovery-restore-flow.spec.js`: 실제 `api/recovery`·`api/customer-orders` 핸들러와 실제 콜렉터 코드로 노트북 키 등록 → 새 브라우저 되살리기 → 내 주문. **이벤트 로그 스냅샷에만 있는 주문이 되살린 브라우저에서 빠졌다.** 조회는 현재 행과 스냅샷 양쪽으로 권한을 판정하는데 등록(`recoveryBoundSet_`)은 현재 행만 결박했다. 등록 시 그 키의 해시를 담은 스냅샷도 결박하도록 고쳤다(`historicCustomerOrdersForHash_`, 해시로 TextFinder 검색이라 이벤트 시트 전체를 읽지 않음). **콜렉터 변경이라 Apps Script 새 버전 배포와, 원래 키를 가진 기기에서의 재등록이 있어야 기존 등록에 반영된다.**

이 결함만으로는 현재 행 주문까지 0건인 것이 설명되지 않는다. 조회 응답의 건수를 모르기 때문이다. 조회 로그에 반환 건수를 남기도록 했다(`[customer-orders] list_result {count, groupScoped}`, 개인정보 없음).

### 조치 (`7d4845d`, 웹 배포 `dpl_7T3jo6hVvzUDV42XLWvpnuvXBeMP`)

- 웹: 조회 반환 건수 로그
- 콜렉터(배포 대기): 스냅샷 주문 결박
- 검증: 619/619, 196/196(신규 브라우저 복구 흐름 2건 포함), 빌드, 운영 5개 경로 200

### 다음

고객이 같은 모바일 Chrome에서 "주문 이력 다시 불러오기"를 한 번 누르면, 로그의 건수로 서버가 0건을 주는지 N건을 주는지 확정된다. N건이면 화면 쪽, 0건이면 콜렉터 쪽이다. 운영 시트 읽기 전용 확인 권한이 있으면 고객 조작 없이 확정할 수 있다.

## 9차 (카톡 00:50~00:51, "다시 불러오기 → 0")

- `list_result {"count":0}` ×5 (00:48:57~00:50:17). **서버가 0건을 돌려준다.** 화면 쪽 문제는 아니다
- 남은 가설 둘: (1) 19:15 되살리기 뒤 모바일 키가 또 바뀜 (2) 콜렉터가 승계를 조회에 적용하지 못함
- 운영 콜렉터 직접 조회는 세션 권한 정책(자격증명 추출 차단)에 막혔다. 내려받은 환경 파일에도 수집기 URL·토큰은 `[SENSITIVE]`로만 온다

### 조치 (`f7aaf90`, 웹 배포 `dpl_A4YSGWwv2T3QncBYLU8vJojZsQKj`)

- 관리자 앱 "복구 상태 점검(읽기 전용)": 관리자 PIN 확인 뒤, 로그인 전화번호와 64자리 복구 코드로 콜렉터 `customer_orders`를 고객 브라우저와 똑같이 호출해 반환 주문(ID·상품명·상태)만 보여 준다. 쓰기 없음. Apps Script 배포 불필요
- 판정: 12:22 관리자 재연결 대상인 옛 모바일 코드 `ce270b…476b8f29` + 01037474754로 점검 → 경기미 `order-1789458468283405584`가 나오면 승계 조회는 정상이므로 (1), 0건이면 (2)
- 검증: 620/620, 196/196, 빌드, 운영 5개 경로 200

## 10차 — 근본 원인 확정: 전화번호 칸이 숫자로 저장됨 (2026-09-24)

관리자 "복구 상태 점검" 결과(고객 12:15, 12:22):

- 01037474754 + `ce270b…476b8f29`(12:22 관리자 재연결 대상, 승계 기록 존재) → **0건**
- 01037474754 + 모바일 현재 코드 `…d98e9141` → **0건**

승계 기록이 있는 코드조차 0건이므로 키 문제가 아니다. 원인: 구글 시트는 서식 없는 칸에 숫자만 있는 문자열을 쓰면 숫자로 바꾼다. `01037474754`는 `1037474754`로 저장된다. 콜렉터는 이벤트·설문 시트의 전화번호 칸만 텍스트 서식(`@`)으로 지정했고(그마저 지정 당시 있던 행까지만), **주문 내역 시트의 연락처 칸(C)은 지정한 적이 없다.** 그래서 전화번호로 거르는 내 주문 조회가 주문 내역 행을 모두 버렸다. 키·승계는 판단되기도 전이다. 노트북이 정상처럼 보인 것은 브라우저에 남은 로컬 사본을 보여 줬기 때문이고, 사본이 없는 새 브라우저는 늘 0건이었다.

지금까지 "내 주문 조회 전부 200"으로 기록한 확인들은 응답 성공만 봤고 건수는 보지 않았다. 9/23 `list_result` 건수 로그를 넣기 전까지 이 결함은 로그에 드러나지 않았다.

### 조치 (`4ed9df1`, 웹 배포 `dpl_GQWfE9YskF6qnTKJyomoQSTiJ3Tk`, 자산 `index-a2XOL2GU.js` SHA-256 `42b42ea0cfdedc0622688d0dc172f403ce1a6d9b23831fb8c9e3c083a086a1bb`)

- 콜렉터 `samePhone_`: 앞자리 0을 떼고 비교 → 이미 숫자로 저장된 기존 행도 즉시 조회된다
- 콜렉터 `textCell_`: 새 주문 행·주문 스냅샷의 숫자뿐인 전화번호는 텍스트로 저장
- 콜렉터 배포 설정 보관: 실제 값으로 한 번 실행되면 `SPREADSHEET_ID`·`INGEST_TOKEN`을 스크립트 속성에 저장하고, 이후 자리표시자 그대로 붙여 넣어도 저장된 값을 쓴다(스크립트 캐시 6시간, 속성 일일 한도 보호). **이번 배포는 두 줄을 한 번 더 되돌려야 하고, 다음부터는 필요 없다**
- 관리자 점검: 붙여 넣은 코드에서 공백·보이지 않는 문자 제거, 오류를 점검 칸 안에 표시
- `tests/phone-cell-coercion.test.js`: 시트가 돌려주는 형태(숫자)로 저장한 전화번호로 현재 행·스냅샷·승계 조회를 검증. **이전 콜렉터에서 실패하고 수정본에서 통과**
- 함께 배포되는 이전 수정: 스냅샷 주문 결박(`7d4845d`), 시트 생성 경쟁(`dd1302d`)
- 검증: 625/625, 196/196, 빌드, 운영 5개 경로 200

### 배포 후 확인

1. 관리자 점검: 01037474754 + `ce270b…476b8f29` → 경기미 `order-1789458468283405584`가 나와야 한다
2. 모바일 내 주문 "다시 불러오기" → `list_result` 건수가 0보다 커야 한다. 모바일 키가 19:15 이후 바뀌었다면 확인번호로 한 번 더 되살린다

## 11차 — 배포 후 확인: 해결 (2026-09-24 12:40~12:43)

고객이 Apps Script를 `main` 최신(`19d30d4` 시점, 전화번호 비교 수정 포함)으로 새 버전 배포.

| 시각 | 요청 | 결과 |
| --- | --- | --- |
| 12:13~12:22 (배포 전) | `customer-orders` list ×6, `recovery_check` ×2 | 모두 `count: 0` |
| 12:40:04 (배포 후) | `recovery_check` 01037474754 + `ce270b…476b8f29` | `count: 2` — 경기미 `order-1789458468283405584`(9/23 12:22 관리자 재연결 대상), 탐사 메모지 |
| 12:41:33 | 모바일 `customer-orders` list | `count: 28` — 9/23 19:15 확인번호 되살리기로 결박된 28건과 일치 |

고객 확인: "모바일 내 주문 들어가 보니 그동안 떴어야 했던 상품들 여러 개 떴습니다."

결론: 9/17부터의 "노트북은 되는데 모바일은 안 된다"는 주문 내역 시트 연락처 칸의 숫자 변환 하나가 원인이었다. 관리자 재연결(9/23 12:22)과 확인번호 되살리기(9/23 19:15)는 처음부터 올바르게 기록돼 있었고, 조회의 전화번호 거르기 단계에서 전부 가려져 있었다. 모바일 키도 19:15 이후 바뀌지 않았다.

### 남은 것

- 서버 포화: 12:42:03 `customer-orders` list 504 1건, `/api/collect` 504 반복, 12:19:42 `group-ops` snapshot 504. 스크립트 잠금 경쟁으로 보이며 채팅 5초 갱신 품질(A-01)과 연결된다. 별도 조사
- 스냅샷에만 있는 옛 주문: 9/23 등록 행은 스냅샷 결박 수정 전에 만들어져 이벤트 로그에만 있는 주문은 되살리기에 포함되지 않았다. 원래 키를 가진 기기에서 확인번호를 다시 등록하면 포함된다
- 하루 뒤에도 모바일 내 주문이 유지되는지 확인한 뒤 최종 검수 재요청
- 공유: 스프레드시트 "uptwoyou data"에 dev@bottlecorp.kr 편집자 추가됨(고객 캡처). 직접 배포에는 Apps Script 스크립트 ID와 배포 ID가 더 필요하다
