# 2026-09-10 20:40 이후 오류 수정과 재발 방지

## 범위와 확인된 증상

사용자가 최신 오류 수정과 재발 방지를 요청했다. 계약상 입금 상태·데이터 연속성·모바일 호환의 오류 수정과 앞서 승인된 관리자 기능의 검수 범위다. 스크린샷의 메시지는 증상 자료로 취급했다.

- 20:40~20:41 메모지 그룹의 입금 요청이 주문 저장 확인 단계에서 차단됐다.
- 20:38~20:39 노트북 사장님 상품 관리에는 맥주 2건과 아메리카노가 남아 있다.
- 20:47 추가 제보: 양파는 사용자 모드로 등록했고 노트북 내 주문에 남아 있다. 카드 표시만으로 중앙 그룹·주문 연결이나 수정 권한을 확정할 수 없다.
- 20:49 중앙 읽기에서 메모지 주문 2건은 각각 수량 1, 유효한 그룹·참여자·예약 연결, 입금대기 버전 1이었다. 검수 목적으로 고객 주문·입금·상품 상태를 변경하지 않았다.

## 수정

1. **중단된 주문 처리 기록:** 서버에 저장된 주문과 브라우저의 중단 기록이 함께 남아 입금 요청을 막는 상황을 실제 API·GAS 코드와 가상 시트로 재현했다. 주문 ID, 그룹, 참여자, 예약 ID·종류·수량이 모두 일치한 중앙 응답으로만 비활성 처리 기록을 정리한다. 활성 추가 주문이나 불일치 기록은 보존한다.
2. **실패를 성공으로 저장하던 표식:** 상품·주문 저장 거절은 성공 확인과 별도로 보관한다. 같은 거절을 무한 자동 반복하지 않고 명시적 재시도를 허용한다. 과거 실패 표식 때문에 주문 완료 화면으로 건너뛰는 경로도 차단했다. 조회·마이그레이션으로 표시 값이 바뀌어도 미확인 과거 상품을 임의로 재게시하지 않는다.
3. **늦은 응답:** 과거 주문 조회가 새로운 저장 확인·입금 상태를 덮지 못하게 했고, 느린 그룹 조회가 입금 버튼을 이전 단계로 되돌리지 못하게 했다. 버튼을 누를 당시의 상태와 서버 상태가 다르면 다른 입금 단계로 해석하지 않는다.
4. **재시도와 화면 이동:** 실패 후 보조 조회를 기다리느라 재시도 버튼이 잠기는 문제를 수정했다. 주문 확인 중 방·프로필을 떠나면 아직 보내지 않은 입금 변경은 중단한다. 이미 전송한 요청의 확인 기록은 보존한다.
5. **상품 표시:** 로컬 목록과 실제 중앙 상품 목록을 구분한다. 서버에서 확인되지 않은 상품을 게시 완료로 표시하지 않으며, 관리 키가 있다는 이유만으로 중앙 상품·주문 조회가 성공했다고 안내하지 않는다.

## 검수 기준

`pnpm verify:release`가 단위·API·실제 GAS 코드 검수, 프로덕션 빌드, Chromium·WebKit 회귀 검수를 순서대로 실행한다. 기존 CI의 전체 테스트에도 새 사례가 포함된다. 현재 작업 트리의 변경은 커밋·푸시하지 않았으므로 원격 CI 실행 완료로 표현하지 않는다.

회귀 사례에는 정상 주문+중단 기록, 예약 불일치, 활성 추가 주문, 오래된 입금 응답, 화면 이탈, 실패 이후 재시도 버튼, 중앙에 없는 상품, 정규화 이후 잘못된 재게시를 포함한다. 별도 실제 HTTP 테스트는 Google 방식 리다이렉트, 요청 처리 전·후 잘못된 상태 응답, 헤더·본문 지연, 같은 요청 ID의 중복 처리를 검사한다.

이 검수는 실제 API/GAS 코드를 실행하되 저장소는 가상 시트다. 고객의 실제 노트북·휴대전화에서 성공했다는 증거와 구분한다. Google 저장소의 간헐적 응답 이상 자체가 사라졌다고 주장하지 않는다.

## 남은 실사용 확인

맥주·아메리카노의 중앙 원본은 이전 정확한 ID 조회에서 확인되지 않았다. 양파에는 과거 표시용 주문 기록이 있지만 중앙 그룹·예약 연결은 확인되지 않았다. 새 스크린샷은 원래 브라우저에 기록이 남아 있다는 증거이며, 임의로 소유권·주문·입금 기록을 만들 근거는 아니다. 원본과 관리 키를 보존하고 기존 수정·확인 경로에서 검증해야 한다.

## 배포 결과

- 단위·API 검수 **519개**, 모바일 Chromium·WebKit 회귀 **176개** 통과. 프로덕션 빌드와 `git diff --check` 통과. 검수 시작 시 저장한 소스 120개와 배포 직전 소스의 해시 차이는 없었다.
- Vercel 후보 `dpl_917XChYiMMzTgNcvHZAWnFaEXxo1` / `https://o2o-faoigpq0x-malshues-projects.vercel.app`를 검수한 뒤 운영 `https://o2o-ten.vercel.app`으로 승격했다. Apps Script는 기존 v23을 유지했다.
- **22:02:57 KST** 운영 `/`, `/customer`, `/owner`, `/admin`, `/dashboard`가 HTTP 200과 `index-D8P5Xfk8.js`를 반환했다. 후보와 운영 JS의 SHA-256은 동일하다: `241bca257f733b62e048c4c764450c9866eb2f3e6258c2065d58fad80bedb5c8`.
- 후보 중앙 공개 상품 조회 HTTP 200 / 46건 / 약 12.9초, 운영 조회 HTTP 200 / 46건 / 약 7.1초. 운영 Chromium·WebKit 각각 사용자·사장님·관리자 초기 화면 렌더링 성공, 페이지 예외 0건. 정상 PIN 로그인과 실제 고객 주문 변경을 운영에서 시험하지 않았다.
- 배포 직후 이 배포의 5xx 로그 조회 결과 0건. 짧은 관찰 구간이며 장기 무오류를 의미하지 않는다. Google 측 간헐 응답 문제는 계속 구분해 관찰해야 한다.
- **22:03:33 KST** 중앙 읽기에서 메모지 두 주문은 검수 전과 같은 입금대기·주문 버전 1·수량 1이었다. 맥주 2개·아메리카노는 공개 상품과 그룹 모두 0건, 양파는 공개 상품 1건·그룹 0건이었다. 이 네 상품의 원래 브라우저 기록 복구를 완료했다고 보고하지 않는다.
- 기존 작업 보존용 시작 백업: `/tmp/o2o-recurrence-20260910-204833.tgz` (권한 600). 커밋·푸시·카카오톡 발송은 하지 않았다.

## 카카오톡 초안 — 미전송

안녕하세요. 반복된 오류로 확인을 여러 번 부탁드려 죄송합니다. 주문이 저장돼 있어도 이전 처리 기록 때문에 입금 요청이 막히는 문제와, 저장 실패를 완료로 취급하거나 늦은 응답이 새 상태를 덮는 문제를 수정해 운영에 반영했습니다. 모바일 두 브라우저에서 실패·재시도 상황까지 검수했습니다.

다만 맥주·아메리카노는 서버 상품 기록이 없고, 양파는 기존 상품·주문 표시 기록과 서버 그룹의 연결이 확인되지 않아 복구 완료로 안내드리기는 어렵습니다. 보내주신 노트북 기록은 확인 자료로 반영했습니다. 원래 브라우저의 기록은 삭제하거나 상품을 새로 등록하지 말고 보존 부탁드립니다. 새로고침한 화면에서 입금 요청을 다시 확인해 주세요.

## 근거

`.vercel/recurrence-20260910/`의 `memo-central-before.json`, `central-after.json`, `tested-source-manifest.json`, `unit-tests.log`, `e2e-tests.log`, `payment-retry-ui.log`, `final-race-tests.log`, `build.log`, `candidate-api.json`, `production-verification.json`, `production-browser.json`, `production-5xx.jsonl`. 수정 전 입금 오류 재현은 `/tmp/o2o-preflight-before.log`, 실제 API·GAS의 지연 응답·화면 이탈 검수는 `/tmp/o2o-preflight-ordering-final.log`에 남겼다. 로그와 문서에 인증 비밀이나 고객 연락처를 복제하지 않는다.
