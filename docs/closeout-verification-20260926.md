# 2026-09-26 운영 검증 및 인수 상태

이 문서를 현재 상태의 기준으로 사용한다. 9월25일의 수정·배포 상세 근거는 `release-verification-20260925.md`, 이전 검수 이력은 `contract-closeout-20260914.md`에 보존했다.

## 범위 및 판정

계약 초안의 A-05·A-07에 해당하는 주문·입금 데이터 정합성 확인과 A-12의 최신 백업·인수 자료 준비를 수행했다. 운영 데이터는 읽기만 했으며 주문, 입금, 공유, 트리거, 프로젝트 설정을 변경하지 않았다. 이번 작업은 문서·백업 작업이므로 추가 코드 배포는 하지 않았다.

개발·배포 기록과 최신 데이터 검증 자료는 준비됐지만, 인증된 운영 화면의 전체 흐름, 실제 두 기기 채팅·알림과 모바일 공유, 고객 최종 검수·인수 승인은 미완료이다. 계약 전체 완료나 모든 오류가 없다는 판정은 하지 않는다. 저장소 계약서는 협의용 초안이며 서명된 최종본이 제공되면 우선한다.

## 최신 운영 읽기

2026-09-26 23:27 KST 점검:

- 공개상품: HTTP200, `ok=true`, 활성46건, 5,652ms.
- 집계: HTTP200, `ok=true`, 집계대상 이벤트15,468건, 8,536ms.
- 위 두 요청에서 실패 응답은 없었다. 제한된 읽기 결과이므로 운영 전체 무오류·응답시간 보장으로 일반화하지 않는다.

운영 주소는 `https://o2o-ten.vercel.app`이다. 마지막 확인 배포는 `dpl_3ZBwQodoBMXmmhcWfwJ6C9a8CU11`, 9월25일15:51:13 KST 생성, 웹 API 수정 커밋 `decec3a`이다. Apps Script 버전30은 소유자 측의 9월25일15:38 배포 완료 회신에 근거한다. dev 계정의 배포 권한 제한으로 버전 내용·실행 사용자·접근 설정을 독립 확인한 것으로 표현하지 않는다.

## 최신 비공개 데이터 백업

권한 있는 Google Sheets 내보내기로 최신 XLSX를 로컬 비공개 인수 폴더에 저장했다. 원본 스프레드시트와 기존 백업은 변경하지 않았다.

- 파일: `/Users/seungsoohan/Documents/O2O-Handover/uptwoyou-data-20260926.xlsx`
- 크기: 3,451,025 bytes, 파일 권한600.
- SHA-256: `3937b9712b79063bb8b3fed80b64e875fc5334cfd9e064e07bfb2127b9029747`
- ZIP CRC 검사 정상, openpyxl 읽기 전용 재열기 정상, 워크시트12개.
- 주문54건, 그룹27건, 참여자50건, 상태 이력202건, 채팅13건, 공개상품 원본61건, 설문15건, 이벤트 원본15,784건, 복구 등록2건을 확인했다. 공개상품 원본 건수는 삭제·비공개 기록을 포함할 수 있어 활성상품 API46건과 다른 지표다.
- 고객 정보·권한 해시가 포함되므로 공개 GitHub·일반 채팅에 첨부하지 않는다. 본 문서는 백업 내용의 요약만 포함한다.

이 파일은 시점 데이터 백업이다. Apps Script 속성, 트리거, 계정 권한이나 브라우저 관리키의 백업까지 대신하지 않으며 운영 복원 시험은 하지 않았다. 최신 인수 시 비밀값·권한 전달은 별도 보안 경로와 실제 수령 확인이 필요하다.

## 입금 상태 및 집계 대조

취소되지 않은 `group`·`purchase` 주문45건의 `groupId`와 `participantActorId`(없는 경우 `visitorId`)로 중앙 참여자 기록을 정확히 연결했다. 45건 모두 연결됐으며 저장된 주문의 입금 상태와 참여자의 입금 상태가 전부 일치했다. 이 집합에서 그룹 연결 없음·참여자 없음·상태 불일치는0건이다. 전체 주문ID, 그룹ID, 그룹·참여자 복합키의 중복도0건이다.

제보 상품명에 해당하는 메모지4건, 참치4건, 퍼실5건, 맥주2건도 위 일치 집합에 포함된다. 개인 식별자와 연락처는 보고서에 기록하지 않았다.

이 검사는 백업의 원시 저장값 대조이다. 현재 로그인 사용자의 권한, 주문 조회 API의 예약·소유권 검증, 화면 렌더링, 요청·취소·완료 버튼의 실시간 변경이나 각 과거 제보의 해결을 증명하는 것으로 확대하지 않는다.

집계 코드와 같은 순서로 이벤트ID 중복4건 및 내부 대시보드 이벤트312건을 제외하면15,468건이다. 공개 집계 API의15,468건과 일치했다. 원본 이벤트는 삭제하지 않았다.

## 자동검증 근거

9월25일 기능 수정 당시 추적 대상 단위·API·Apps Script636/636, Chromium·WebKit200/200, 빌드·의존성 점검이 통과했다. 뒤이은 stats 대기시간 한정 수정은 관련 회귀41/41이 통과했다. 한정 수정 뒤 전체836건을 다시 실행한 것으로 표현하지 않는다. 이번 문서·백업 작업에서는 코드가 바뀌지 않아 해당 검사를 불필요하게 반복하지 않았다.

## 남은 확인과 인수

1. 관리자 인증: Codex 브라우저와 Chrome 모두 관리자 인증 전이며, 연락처가 비어 있는 프로필 화면이다. 사용자가 화면에서 검수용 프로필과 관리자 PIN 인증을 완료해야 한다. PIN을 채팅에 제출하거나 인증을 우회할 필요는 없다.
2. 인증 후 운영 검수: 정확한 기존 제보 그룹에서 중앙값과 그룹·내 주문·관리자 화면을 읽기 대조한다. 상태 변경 시험은 승인된 테스트 그룹에서 수행하고 실제 고객 주문을 시험 삼아 변경하지 않는다.
3. 두 기기 채팅·알림 및 모바일 OS 공유 시트: 격리 브라우저 회귀 결과와 별도로 실기기 결과를 확인한다.
4. 인수: 소스, 환경설정 이름, 최신 데이터 백업, 비밀값·계정 관리권한을 수령자가 확인한다. 자료 준비와 수령 완료를 구분한다.
5. 고객 최종 검수: “네 확인 감사합니다”는 수신 응답이며 전체 A-01~A-12 합격 회신으로 간주하지 않는다.

비공개 상세 증거는 로컬 `.vercel/closeout-20260926/`에 있으며 공개 소스 압축본에 넣지 않는다. 고객에게 보낼 문안은 같은 인수 폴더의 `O2O-kakao-20260926.txt`에 초안으로 준비하며 직접 발송하지 않는다.
